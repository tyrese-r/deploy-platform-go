# Deploy Platform Go

A PaaS project

The current state can only build and run images from a dockerfile as a single command

## Usage


`go run ./cmd/deploycontrol -path=./example-projects/hello-world-server`

By default it will run it on port 8080